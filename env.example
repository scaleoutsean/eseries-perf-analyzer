# Sample environment variables for docker-compose

# Collector service
USERNAME=monitor
PASSWORD=

# SANtricity API endpoints (controllers) - space-separated list if two
API="5.5.5.5 6.6.6.6"

# InfluxDB settings are auto-generated by influxdb-init container
# and loaded from secrets/influxdb-tokens.env via docker-compose.yaml

# MinIO root credentials and ports
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# S3 bucket name for InfluxDB tiered storage
S3_BUCKET=influxdb
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=

# nginx proxy ports
NGINX_S3_API_PORT=9000          # proxies MinIO API (nginx -> s3:9000)
NGINX_S3_CONSOLE_PORT=9001      # proxies MinIO console (nginx -> s3:9001)
NGINX_INF_API_HTTP_PORT=18181   # proxies InfluxDB API HTTPS (nginx -> influxdb:8181) - this is InfluxDB on HTTPS/8181!
NGINX_INF_UI_HTTP_PORT=18443    # proxies InfluxDB3 UI HTTPS (nginx -> influxdb3-ui:443)
NGINX_API_A_HTTP_PORT=2001      # proxies E-Series SANtricity UI API HTTPS for Controller A (nginx -> api1:8443)
NGINX_API_B_HTTP_PORT=2002      # proxies E-Series SANtricity UI API HTTPS for Controller B (nginx -> api2:8443)
NGINX_GRAFANA_UI_HTTP_PORT=3443 # proxies Grafana UI HTTPS (nginx -> grafana:3000) - Grafana HTTPS container keeps port 3000


