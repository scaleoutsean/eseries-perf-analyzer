global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'eseries_collector'
    static_configs:
      # One target per collector service in two-array-docker-compose.yml.
      - targets: ['collector1:9080', 'collector2:9080']
